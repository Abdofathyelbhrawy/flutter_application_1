// utils/pdf_download_stub.dart
// Mobile stub — not used on mobile (uses Printing.sharePdf instead)
import 'dart:typed_data';

void downloadPdfWeb(Uint8List bytes, String filename) {
  // No-op on mobile — this file is never actually called
}
